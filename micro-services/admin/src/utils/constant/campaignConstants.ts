// campaign type constants and enums
export const campaignCategory = ['marketing', 'promotion', 'discount'];
export type campaignCategoryType = 'marketing' | 'promotion' | 'discount';

// campaign status constants and enum
export const campaignStatusCategory = [
  'pending',
  'in progress',
  'stopped',
  'done',
];
export type campaignStatusCategoryType =
  | 'pending'
  | 'in progress'
  | 'stopped'
  | 'done';
